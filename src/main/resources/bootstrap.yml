server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          username: ${GIT_USER}
          password: ${GIT_TOKEN}
          default-label: main
  application:
    name: byron-config-server